palettes:
  - name: global_palette
    images: automatic

converts:
  - name: sprites
    palette: global_palette
    images:
%REPLACEME%
outputs:
  - type: c
    include-file: gfx.h
    palettes:
      - global_palette
    converts:
      - sprites


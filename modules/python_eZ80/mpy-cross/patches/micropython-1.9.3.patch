--- a/mpy-cross/main.c
+++ b/mpy-cross/main.c
@@ -44,9 +44,13 @@
 
 // Heap size of GC heap (if enabled)
 // Make it larger on a 64 bit machine, because pointers are larger.
+#if defined(__EMSCRIPTEN__)
+long heap_size = 16 * 1024 * 1024;
+#else
 long heap_size = 1024*1024 * (sizeof(mp_uint_t) / 4);
-
-STATIC void stderr_print_strn(void *env, const char *str, mp_uint_t len) {
+#endif
+
+STATIC void stderr_print_strn(void *env, const char *str, size_t len) {
     (void)env;
     ssize_t dummy = write(STDERR_FILENO, str, len);
     (void)dummy;
@@ -179,8 +183,24 @@
 
     pre_process_options(argc, argv);
 
+    #if defined(__EMSCRIPTEN__)
+    // A fresh compiler instance owns this bounded arena for one compilation.
+    if (heap_size < 16 * 1024 || heap_size > 16 * 1024 * 1024) {
+        fprintf(stderr, "Compiler heap must be between 16 KiB and 16 MiB\n");
+        return 1;
+    }
+    #endif
     char *heap = malloc(heap_size);
+    if (heap == NULL) {
+        fprintf(stderr, "Unable to allocate compiler memory\n");
+        return 1;
+    }
     gc_init(heap, heap + heap_size);
+    #if defined(__EMSCRIPTEN__)
+    // setjmp cannot expose WebAssembly locals to the old conservative GC.
+    // Keep explicit frees; fail with MemoryError instead of scanning roots.
+    MP_STATE_MEM(gc_auto_collect_enabled) = 0;
+    #endif
 
     mp_init();
 #ifdef _WIN32
--- a/py/persistentcode.c
+++ b/py/persistentcode.c
@@ -271,7 +271,7 @@
         } else {
             obj_type = 'b';
         }
-        mp_uint_t len;
+        size_t len;
         const char *str = mp_obj_str_get_data(o, &len);
         mp_print_bytes(print, &obj_type, 1);
         mp_print_uint(print, len);
--- a/py/vm.c
+++ b/py/vm.c
@@ -750,8 +750,8 @@
                     obj = mp_getiter(obj, iter_buf);
                     if (obj != MP_OBJ_FROM_PTR(iter_buf)) {
                         // Iterator didn't use the stack so indicate that with MP_OBJ_NULL.
-                        sp[-MP_OBJ_ITER_BUF_NSLOTS + 1] = MP_OBJ_NULL;
-                        sp[-MP_OBJ_ITER_BUF_NSLOTS + 2] = obj;
+                        sp[-(mp_int_t)MP_OBJ_ITER_BUF_NSLOTS + 1] = MP_OBJ_NULL;
+                        sp[-(mp_int_t)MP_OBJ_ITER_BUF_NSLOTS + 2] = obj;
                     }
                     DISPATCH();
                 }
@@ -761,10 +761,10 @@
                     DECODE_ULABEL; // the jump offset if iteration finishes; for labels are always forward
                     code_state->sp = sp;
                     mp_obj_t obj;
-                    if (sp[-MP_OBJ_ITER_BUF_NSLOTS + 1] == MP_OBJ_NULL) {
-                        obj = sp[-MP_OBJ_ITER_BUF_NSLOTS + 2];
+                    if (sp[-(mp_int_t)MP_OBJ_ITER_BUF_NSLOTS + 1] == MP_OBJ_NULL) {
+                        obj = sp[-(mp_int_t)MP_OBJ_ITER_BUF_NSLOTS + 2];
                     } else {
-                        obj = MP_OBJ_FROM_PTR(&sp[-MP_OBJ_ITER_BUF_NSLOTS + 1]);
+                        obj = MP_OBJ_FROM_PTR(&sp[-(mp_int_t)MP_OBJ_ITER_BUF_NSLOTS + 1]);
                     }
                     mp_obj_t value = mp_iternext_allow_raise(obj);
                     if (value == MP_OBJ_STOP_ITERATION) {
